# 🔄 Обновление страницы Аналитики продаж

## ✅ Что изменилось

Страница **Аналитика продаж** теперь работает с **реальными данными из Ozon API**!

---

## 🎯 Основные изменения

### ❌ Было:
- Моковые (фейковые) данные
- Нереалистичные графики
- Статичные таблицы

### ✅ Стало:
- **Реальные данные** из Ozon API
- Данные получаются через `/v2/posting/fbo/list`
- Финансовые данные с `financial_data`
- **Живые графики** на основе реальных заказов
- **Динамическая таблица** с реальными товарами
- **Расчёт прибыли** с учётом себестоимости

---

## 📊 Что показывает страница

### 1. KPI Метрики (сверху)

#### Выплата продавцу
- Сумма всех выплат продавцу за период
- Рассчитывается из `financial_data.products[].payout`

#### Заказы
- Общее количество заказов
- Статусы: delivered + delivering + cancelled

#### Средний чек
- Выплата продавцу / количество заказов
- Показывает среднюю сумму на заказ

#### Прибыль
- Выплата продавцу - себестоимость товаров
- Зелёный если прибыль, красный если убыток
- Требует ввода себестоимости в таблице

---

## 📈 Графики

### 1. Выплаты продавцу по дням
- Показывает динамику выплат по дням
- Данные группируются по дате заказа
- Smooth линия с area fill

### 2. Заказы по дням
- Количество заказов по дням
- Помогает увидеть пики продаж

### 3. Статистика по складам
- BarChart с заказами по складам
- Топ-10 самых активных складов
- Данные из `analytics_data.warehouse_name`

---

## 📋 Таблица продаж по товарам

### Колонки:

1. **SKU** - артикул товара
2. **Название товара** - из `products[].name`
3. **Кол-во** - количество проданных единиц
4. **Цена** - цена товара из `financial_data.products[].price`
5. **Выплата продавцу** - из `financial_data.products[].payout`
6. **Комиссия** - из `financial_data.products[].commission_amount`
7. **Статусы** - теги с количеством по статусам:
   - ✅ Доставлено (delivered)
   - 🚚 Доставляется (delivering)
   - ❌ Отменено (cancelled)
8. **Себестоимость ₽/шт** - **InputNumber для ввода**
9. **Прибыль** - Выплата - (Себестоимость × Количество)
10. **Склады** - список складов для товара

### Возможности таблицы:
- ✅ Сортировка по всем колонкам
- ✅ Пагинация (10/20/50/100 строк)
- ✅ Ввод себестоимости для расчёта прибыли
- ✅ Автоматический пересчёт прибыли и KPI

---

## 🔧 API Запросы

### Используемые endpoints:

#### 1. `/v2/posting/fbo/list` (useFboPostings2)
```typescript
{
  offset: 0,
  limit: 1000,
  filter: {
    since: '2025-10-01T00:00:00Z',
    to: '2025-10-27T23:59:59Z',
  },
  with: {
    analytics_data: true,
    financial_data: true
  }
}
```

**Что получаем:**
- Список всех заказов за период
- Данные о товарах в заказе
- Финансовые данные (цены, комиссии, выплаты)
- Аналитические данные (склады, даты)
- Статусы заказов

---

## 💾 Агрегация данных

### Данные агрегируются по SKU:

```typescript
// Для каждого товара собираем:
{
  sku: number,           // Артикул
  name: string,          // Название
  quantity: number,      // Количество
  price: number,         // Цена
  revenue: number,       // Выручка
  commission: number,    // Комиссия
  payout: number,        // Выплата
  delivered: number,     // Доставлено
  cancelled: number,     // Отменено
  delivering: number,    // Доставляется
  warehouses: string[],  // Склады
}
```

### Расчёт прибыли:

```typescript
const cost = costs[sku] || 0;              // Себестоимость (вводится вручную)
const totalCost = cost * quantity;         // Общая себестоимость
const profit = payout - totalCost;         // Прибыль
const margin = (profit / payout) * 100;    // Маржа в %
```

---

## 🔄 Фильтр по датам

### Работает в реальном времени:

1. Выбираете новый диапазон дат
2. Автоматически запускается новый запрос к API
3. Все данные пересчитываются:
   - KPI метрики
   - Графики
   - Таблица

### По умолчанию:
```typescript
const [dateRange, setDateRange] = useState<[Dayjs, Dayjs]>([
  dayjs('2025-10-01'),
  dayjs('2025-10-27'),
]);
```

---

## 🔑 API Credentials

### Временно захардкожены в коде:

```typescript
const CLIENT_ID = '3088921';
const API_KEY = 'c2b734ef-870d-45b5-a975-dc61e723ed9e';
```

### В будущем можно:
1. Вынести в `.env` файл
2. Добавить форму авторизации
3. Хранить в localStorage
4. Использовать context/store

---

## 💡 Как использовать

### 1. Откройте страницу:
```bash
npm run dev
# Перейдите на вкладку "Аналитика продаж"
```

### 2. Выберите период:
- Кликните на фильтр дат
- Выберите новый диапазон
- Данные автоматически обновятся

### 3. Введите себестоимость:
- В таблице найдите колонку "Себестоимость ₽/шт"
- Введите стоимость закупки товара
- Прибыль и KPI автоматически пересчитаются

### 4. Анализируйте данные:
- Смотрите графики по дням
- Сравнивайте склады
- Сортируйте таблицу
- Ищите самые прибыльные товары

---

## 📊 Примеры данных

### Пример товара в таблице:

```
SKU: 123456789
Название: Наушники Bluetooth TWS
Кол-во: 45 шт
Цена: 1,200 ₽
Выплата продавцу: 48,600 ₽
Комиссия: 10,800 ₽
Статусы: ✅ 40  🚚 3  ❌ 2
Себестоимость: 800 ₽/шт [ввод]
Прибыль: 12,600 ₽ (зелёный)
Склады: Москва, СПб
```

### Расчёт прибыли для примера:

```
Выплата продавцу:  48,600 ₽
Себестоимость:        800 ₽/шт × 45 шт = 36,000 ₽
─────────────────────────────────────────────
Прибыль:           12,600 ₽
Маржа:              26% (12,600 / 48,600)
```

---

## 🎨 Особенности UI

### Loading состояние:
```tsx
if (isLoading) {
  return <Spin size="large" tip="Загрузка данных..." />;
}
```

### Пустые данные:
```tsx
{dailyData.labels.length > 0 ? (
  <LineChart data={dailyData} />
) : (
  <Text>Нет данных за выбранный период</Text>
)}
```

### Цветовое кодирование:
- 🔵 Синий - выплата продавцу
- 🟢 Зелёный - заказы, прибыль
- 🟠 Оранжевый - средний чек
- 🔴 Красный - убыток
- 🟣 Фиолетовый - дополнительные метрики

---

## 🔄 Связь с другими страницами

### Данные можно использовать для:

1. **Калькулятор прибыли**
   - Брать реальные цены из аналитики
   - Использовать историю себестоимости

2. **Воронка продаж**
   - Дополнительные метрики конверсии
   - Связь с просмотрами товаров

---

## ✅ Что работает

- ✅ Реальные данные из Ozon API
- ✅ Фильтр по датам
- ✅ Агрегация по SKU
- ✅ Расчёт всех метрик
- ✅ Графики по дням
- ✅ График по складам
- ✅ Таблица с сортировкой
- ✅ Ввод себестоимости
- ✅ Автоматический пересчёт прибыли
- ✅ Loading состояния
- ✅ Обработка пустых данных

---

## 📚 Исходный код

### Основные файлы:

1. `/src/pages/SalesAnalytics/SalesAnalytics.tsx` (450+ строк)
   - Главный компонент страницы
   - Логика агрегации и расчётов
   - Все useMemo хуки

2. `/src/entities/app/api/posting-fbo.ts`
   - useFboPostings2 hook
   - Запрос к `/v2/posting/fbo/list`

---

## 🚀 Что можно добавить

### В будущем:

1. **Экспорт в Excel**
   - Таблица с расчётами
   - Графики в виде изображений

2. **Сохранение себестоимости**
   - В localStorage
   - В базу данных

3. **Сравнение периодов**
   - Два диапазона дат
   - Показывать изменения

4. **Больше метрик**
   - Возвраты в деньгах
   - Конверсия по складам
   - Топ товаров

5. **Фильтры**
   - По складам
   - По статусам
   - По категориям товаров

---

## 🎉 Готово!

Страница **Аналитика продаж** теперь работает с **реальными данными**!

### Попробуйте:
```bash
npm run dev
# Откройте "Аналитика продаж"
# Выберите период
# Введите себестоимость
# Смотрите реальную прибыль!
```

**Все данные теперь берутся из Ozon API!** ✨
